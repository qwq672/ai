<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 应用主题：使用 Material3 DayNight，确保所有 Material3 style 可用 -->
    <style name="Theme.GenieXDemo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/brand_primary_light</item>
        <item name="colorOnPrimaryContainer">@color/brand_primary_dark</item>
        <item name="colorSecondary">@color/brand_accent</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:windowBackground">@color/bg_app</item>
        <item name="android:statusBarColor">@color/brand_primary</item>
        <item name="android:navigationBarColor">@color/bg_app</item>
    </style>

    <style name="TransparentBottomSheetDialog" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- 主聊天界面顶部 Toolbar -->
    <style name="Widget.GenieX.Toolbar" parent="Widget.Material3.Toolbar.Surface">
        <item name="android:background">@color/brand_primary</item>
        <item name="titleTextColor">@color/white</item>
        <item name="subtitleTextColor">@color/white_70</item>
        <item name="navigationIconTint">@color/white</item>
    </style>

    <!-- 模型/设置页面 Toolbar -->
    <style name="Widget.GenieX.SecondaryToolbar" parent="Widget.Material3.Toolbar.Surface">
        <item name="android:background">@color/bg_card</item>
        <item name="titleTextColor">@color/text_primary</item>
        <item name="subtitleTextColor">@color/text_secondary</item>
        <item name="navigationIconTint">@color/text_primary</item>
    </style>

    <!-- 圆角卡片样式 -->
    <style name="Widget.GenieX.CardView" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardBackgroundColor">@color/bg_card</item>
        <item name="contentPadding">16dp</item>
    </style>

    <!-- 主操作按钮 -->
    <style name="Widget.GenieX.PrimaryButton" parent="Widget.Material3.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">14sp</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <style name="Widget.GenieX.OutlinedButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">13sp</item>
        <item name="cornerRadius">10dp</item>
    </style>

</resources>
